spring:
  jpa:
    database: postgresql
    show-sql: true
    hibernate:
      ddl-auto: create
  datasource:
    url: jdbc:postgresql://localhost:8081/userapi
    username: karen
    password: HelloThere
    driver-class-name: org.postgresql.Driver
    platform: postgres