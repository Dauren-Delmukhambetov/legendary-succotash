spring:
  jpa:
    database: postgresql
    show-sql: true
    hibernate:
      ddl-auto: create
  datasource:
    url: jdbc:postgresql://localhost:5432/userapi
    username: postgres
    password:
    driver-class-name: org.postgresql.Driver
    platform: postgres